[project]
name = "ml-tarflow"
version = "0.1.0"
description = "dataset generator with good distribution"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "torch==2.5.1",
    "torchvision==0.20.1",
    "jupyter>=1.0.0",
    "torch-fidelity==0.3.0",
    "torchmetrics==1.6.0",
]
